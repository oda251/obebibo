<!DOCTYPE html>
<html>
  <head>
    <title>管理画面 - Obebibo</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-100">
    <header class="bg-gray-800 text-white p-4">
      <div class="container mx-auto flex justify-between items-center">
        <%= link_to "Obebibo 管理画面", admin_root_path, class: "text-2xl font-bold" %>
        
        <nav class="flex items-center space-x-4">
          <%= link_to "ダッシュボード", admin_root_path, class: "hover:underline" %>
          <%= link_to "案件管理", admin_campaigns_path, class: "hover:underline" %>
          <%= link_to "応募管理", admin_entries_path, class: "hover:underline" %>
          <%= link_to "配送管理", admin_shipments_path, class: "hover:underline" %>
          <%= link_to "レビュー管理", admin_reviews_path, class: "hover:underline" %>
          <%= link_to "一般サイト", root_path, class: "bg-blue-500 px-3 py-1 rounded hover:bg-blue-600" %>
          <%= link_to "ログアウト", destroy_admin_session_path, method: :delete, class: "hover:underline" %>
        </nav>
      </div>
    </header>

    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded m-4">
        <%= notice %>
      </div>
    <% end %>

    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded m-4">
        <%= alert %>
      </div>
    <% end %>

    <main class="container mx-auto px-4 py-8">
      <%= yield %>
    </main>
  </body>
</html>